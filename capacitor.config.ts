import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ESP32-Firebase-React',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
